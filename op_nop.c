#include "monty.h"

/**
 * op_nop - A function that doesn't do anything.
 *
 * @stack: The head of the stack.
 *
 * Return: Nothing.
*/
void op_nop(stack_t **stack, unsigned int line_number)
{
    (void) stack;
    (void) line_number;
}